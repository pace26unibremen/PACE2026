image: gcc:13  # GCC 14+ with CMake 3.28+, C++20 ready

stages:
  - build
  - test

build-job:
  stage: build
  script:
    - gcc --version && g++ --version && cmake --version  # Verify tools
    - mkdir build/
    - cd build/
    - cmake ..
    - make -j 4
    - mkdir install
    - make DESTDIR=install install
  artifacts:
    expire_in: 1 day
    paths:
      - build/tests/ForestIOTests
      - build/tests/ForestEqualTests
      - build/tests/ForestSubforestTests
      - build/tests/DeleteEdgeActionTests

graph-representation:
  stage: test
  script:
    - cd build/tests/
    - ./ForestIOTests
    - ./ForestEqualTests
    - ./ForestSubforestTests
    - ./DeleteEdgeActionTests
  dependencies:
    - build-job